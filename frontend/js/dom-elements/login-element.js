const loginContainer = document.querySelector('.login__container');
const login = loginContainer.querySelector('.login');
const loginForm = loginContainer.querySelector('.login__form');
const loginInput = loginContainer.querySelector('.login__input');

export { loginContainer, login, loginForm, loginInput };
